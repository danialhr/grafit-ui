# Checkbox Component Metadata
source:
  repository: https://github.com/shadcn-ui/ui
  component: checkbox
  sourcePath: apps/v4/registry/new-york-v4/ui/checkbox.tsx
  commit: 7ee929ac
  version: null
  lastChecked: 2025-02-06
  lastSynced: 2025-02-06

flutter:
  path: lib/src/components/form/checkbox.dart
  class: GrafitCheckbox
  status: implemented

parity:
  target: 100
  current: 100
  gap: []

mapping:
  variants:
    default: default
    secondary: secondary

  props:
    checked: value (bool)
    onCheckedChange: onChanged (ValueChanged<bool>)
    disabled: disabled (bool)
    required: N/A (Flutter uses FormField validator)
    name: N/A (Flutter uses Key)
    id: N/A (Flutter uses Key)

unimplemented: []

adaptations:
  - "ValueNotifier pattern instead of React state"
  - "GestureDetector for tap handling"
  - "Container with decoration for visual styling"
  - "Icon for checkbox indicator"
  - "Form validation handled by FormField widget instead of required prop"
