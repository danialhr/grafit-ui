# Input Component Metadata
source:
  repository: https://github.com/shadcn-ui/ui
  component: input
  sourcePath: apps/v4/registry/new-york-v4/ui/input.tsx
  commit: 7ee929ac
  version: null
  lastChecked: 2025-02-06
  lastSynced: 2025-02-06

flutter:
  path: lib/src/components/form/input.dart
  class: GrafitInput
  status: implemented

parity:
  target: 100
  current: 100
  gap: []

mapping:
  variants:
    default: default

  props:
    type: keyboardType (TextInputType)
    placeholder: hint (String)
    disabled: disabled (bool)
    value: controller (TextEditingController)
    onChange: onChanged (ValueChanged<String>)
    maxLength: maxLength (int)
    prefix: prefix (Widget)
    suffix: suffix (Widget)

unimplemented: []

adaptations:
  - "TextEditingController instead of React state"
  - "TextInputType for type attribute (email, number, text, etc.)"
  - "InputDecoration wrapper for styling instead of CSS classes"
  - "prefix/suffix as Widget parameters instead of slots"
  - "Error state via errorText prop with validation"
  - "File upload is a separate component pattern in Flutter"
